<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Cass Bot ADA" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300..700&display=swap" rel="stylesheet">

    <!-- Android Chrome 192x192 -->
    <link rel="icon" type="image/png" sizes="192x192" href="<%= image_path("android-chrome-192x192.png") %>">

    <!-- Android Chrome 512x512 -->
    <link rel="icon" type="image/png" sizes="512x512" href="<%= image_path("android-chrome-512x512.png") %>">

    <!-- Apple Touch Icon -->
    <link rel="apple-touch-icon" sizes="180x180" href="<%= image_path("apple-touch-icon.png") %>">

    <!-- Favicon 16x16 -->
    <link rel="icon" type="image/png" sizes="16x16" href="<%= image_path("favicon-16x16.png") %>">

    <!-- Favicon 32x32 -->
    <link rel="icon" type="image/png" sizes="32x32" href="<%= image_path("favicon-32x32.png") %>">

    <!-- Legacy favicon -->
    <link rel="shortcut icon" href="<%= image_path("favicon.ico") %>">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body style="background-image: url('<%= image_path('bg-1.jpg') %>');">
    <div class="overlay"></div>
    <%= yield %>

    <footer class="footer">
      <p>&copy; <%= Time.now.year %> Cass Bot ADA. All rights reserved.</p>
    </footer>
  </body>
</html>
